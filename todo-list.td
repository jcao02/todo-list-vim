# TODO list for todo-list-vim plugin

    [ ] Add whole project todo parser
    [ ] Add event handler when the buffer is closed (it needs to exe match none)
    [ ] Read filename.td file when calling the buffer and append those tasks to the buffer
    [ ] Add better priority handling (a dictionary might by useful)
